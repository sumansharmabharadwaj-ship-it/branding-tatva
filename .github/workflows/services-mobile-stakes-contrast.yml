name: Build and verify the compact mobile Stakes contrast
run-name: Services mobile Stakes contrast · ${{ github.sha }}

on:
  push:
    branches:
      - services-mobile-stakes-contrast
    paths:
      - .github/workflows/services-mobile-stakes-contrast.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: branding-tatva-services-mobile-stakes-contrast
  cancel-in-progress: true

jobs:
  build-audit-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Check out the focused branch without the large media archive
        uses: actions/checkout@v4
        with:
          ref: services-mobile-stakes-contrast
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /*
            !/public/videos/*
            !/public/audio/*

      - name: Install the compact Stakes browser contract
        run: |
          python -m py_compile scripts/apply_services_mobile_stakes_gate.py
          python scripts/apply_services_mobile_stakes_gate.py
          git diff --check

      - name: Use Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.28.0 --activate

      - name: Install dependencies and Chromium
        run: |
          pnpm install --frozen-lockfile
          pnpm add --save-dev --lockfile=false playwright@1.54.2
          pnpm exec playwright install --with-deps chromium

      - name: Build the production application
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: pnpm build

      - name: Start the production server
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          pnpm start --hostname 127.0.0.1 --port 3000 > /tmp/services-mobile-stakes.log 2>&1 &
          echo $! > /tmp/services-mobile-stakes.pid
          for attempt in {1..90}; do
            if curl --fail --silent http://127.0.0.1:3000/services > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/services-mobile-stakes.log
          exit 1

      - name: Walk the complete Services journey at three viewports
        env:
          AUDIT_BASE_URL: http://127.0.0.1:3000
        run: node scripts/services_page_gate.cjs

      - name: Stop the production server
        if: always()
        run: |
          if [ -f /tmp/services-mobile-stakes.pid ]; then
            kill "$(cat /tmp/services-mobile-stakes.pid)" || true
          fi
          cat /tmp/services-mobile-stakes.log || true

      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: services-mobile-stakes-contrast-evidence
          path: services-page-audit/*
          if-no-files-found: warn
          retention-days: 7

      - name: Remove one-time transformer and workflow
        run: |
          rm scripts/apply_services_mobile_stakes_gate.py
          rm .github/workflows/services-mobile-stakes-contrast.yml

      - name: Commit the verified compact Stakes contrast
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/sections/Services/MobileStakesDeck.tsx \
            src/sections/Services/WeakBrandingCost.tsx \
            scripts/services_page_gate.cjs \
            scripts/apply_services_mobile_stakes_gate.py \
            .github/workflows/services-mobile-stakes-contrast.yml
          git diff --cached --check
          git commit -m "Compress mobile Stakes into one contrast deck"
          git push origin HEAD:services-mobile-stakes-contrast
