name: Build and verify the compact mobile perception climb
run-name: Services mobile perception climb · ${{ github.sha }}

on:
  push:
    branches:
      - services-mobile-perception-climb
    paths:
      - .github/workflows/services-mobile-perception-climb.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: branding-tatva-services-mobile-perception-climb
  cancel-in-progress: true

jobs:
  build-audit-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Check out the focused branch without the large media archive
        uses: actions/checkout@v4
        with:
          ref: services-mobile-perception-climb
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /*
            !/public/videos/*
            !/public/audio/*

      - name: Install the compact perception browser contract
        run: |
          python -m py_compile scripts/apply_services_mobile_perception_gate.py
          python scripts/apply_services_mobile_perception_gate.py
          git diff --check

      - name: Use Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.28.0 --activate

      - name: Install dependencies and Chromium
        run: |
          pnpm install --frozen-lockfile
          pnpm add --save-dev --lockfile=false playwright@1.54.2
          pnpm exec playwright install --with-deps chromium

      - name: Build the production application
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: pnpm build

      - name: Start the production server
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          pnpm start --hostname 127.0.0.1 --port 3000 > /tmp/services-mobile-perception.log 2>&1 &
          echo $! > /tmp/services-mobile-perception.pid
          for attempt in {1..90}; do
            if curl --fail --silent http://127.0.0.1:3000/services > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/services-mobile-perception.log
          exit 1

      - name: Walk the complete Services journey at three viewports
        env:
          AUDIT_BASE_URL: http://127.0.0.1:3000
        run: node scripts/services_page_gate.cjs

      - name: Stop the production server
        if: always()
        run: |
          if [ -f /tmp/services-mobile-perception.pid ]; then
            kill "$(cat /tmp/services-mobile-perception.pid)" || true
          fi
          cat /tmp/services-mobile-perception.log || true

      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: services-mobile-perception-climb-evidence
          path: services-page-audit/*
          if-no-files-found: warn
          retention-days: 7

      - name: Remove one-time transformer and workflow
        run: |
          rm scripts/apply_services_mobile_perception_gate.py
          rm .github/workflows/services-mobile-perception-climb.yml

      - name: Commit the verified compact perception climb
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/sections/Services/MobilePerceptionClimb.tsx \
            src/sections/Services/PerceptionLadder.tsx \
            scripts/services_page_gate.cjs \
            scripts/apply_services_mobile_perception_gate.py \
            .github/workflows/services-mobile-perception-climb.yml
          git diff --cached --check
          git commit -m "Compress mobile Education into one perception climb"
          git push origin HEAD:services-mobile-perception-climb
