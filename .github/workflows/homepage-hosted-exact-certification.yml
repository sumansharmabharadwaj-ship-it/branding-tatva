name: Homepage V4 hosted exact-source certification
run-name: Homepage hosted certification · ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches:
      - homepage-cinematic-recovery
    paths:
      - .github/workflows/homepage-hosted-exact-certification.yml
      - .github/workflows/homepage-cinematic-recovery-gate.yml
      - .github/workflows/home-v4-loader-timing-gate.yml
      - scripts/*home*.cjs
      - scripts/*cinematic*.cjs
      - scripts/*loader*.cjs
      - src/app/page.tsx
      - src/app/HomePageV4.tsx
      - src/sections/HomeV4/**
      - src/components/**
      - src/lib/**
      - src/app/release-markers/**
      - public/images/**
      - package.json
      - pnpm-lock.yaml
      - next.config.ts
      - vercel.json

permissions:
  contents: write

concurrency:
  group: branding-tatva-homepage-hosted-exact-certification
  cancel-in-progress: true

jobs:
  certify-homepage:
    runs-on: macos-latest
    timeout-minutes: 50

    steps:
      - name: Check out the newest canonical application source without the media archive
        uses: actions/checkout@v4
        with:
          ref: homepage-cinematic-recovery
          fetch-depth: 0
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /*
            !/public/videos/*
            !/public/audio/*

      - name: Resolve the newest deployable source commit
        id: source
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin homepage-cinematic-recovery
          git reset --hard origin/homepage-cinematic-recovery
          sha="$(git log -1 --format=%H -- \
            src public package.json pnpm-lock.yaml next.config.ts \
            postcss.config.mjs tsconfig.json vercel.json)"
          test -n "$sha"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"
          echo "Deployable Homepage source: $sha"

      - name: Use Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.28.0 --activate

      - name: Install the Homepage browser runner
        run: |
          pnpm install --frozen-lockfile
          pnpm add --save-dev --lockfile=false playwright@1.54.2
          pnpm exec playwright install chromium

      - name: Wait for the permanent alias to serve the exact deployable source
        shell: bash
        env:
          EXPECTED_SHA: ${{ steps.source.outputs.sha }}
          EXPECTED_BRANCH: homepage-cinematic-recovery
          PREVIEW_URL: https://branding-tatva-git-homepage-cinematic-recovery-suman22.vercel.app
        run: |
          set -euo pipefail
          node <<'NODE'
          const expectedSha = process.env.EXPECTED_SHA;
          const expectedBranch = process.env.EXPECTED_BRANCH;
          const baseUrl = process.env.PREVIEW_URL.replace(/\/$/, "");
          const deadline = Date.now() + 15 * 60 * 1000;
          function contains(value, needle) {
            if (typeof value === "string") return value.includes(needle);
            if (Array.isArray(value)) return value.some((item) => contains(item, needle));
            if (value && typeof value === "object") {
              return Object.entries(value).some(([key, item]) => contains(key, needle) || contains(item, needle));
            }
            return false;
          }
          while (Date.now() < deadline) {
            try {
              const response = await fetch(`${baseUrl}/api/release`, {
                cache: "no-store",
                headers: { "user-agent": "branding-tatva-homepage-hosted-certification" },
              });
              const text = await response.text();
              let body;
              try { body = JSON.parse(text); } catch { body = { text }; }
              if (response.ok && contains(body, expectedSha) && contains(body, expectedBranch)) {
                console.log(JSON.stringify({ exactSource: true, expectedSha, body }, null, 2));
                process.exit(0);
              }
              console.log(`Alias not exact yet: status=${response.status}, expected=${expectedSha.slice(0, 12)}`);
            } catch (error) {
              console.log(`Alias probe failed: ${error instanceof Error ? error.message : String(error)}`);
            }
            await new Promise((resolve) => setTimeout(resolve, 15_000));
          }
          throw new Error(`Permanent alias did not serve exact Homepage source ${expectedSha} within 15 minutes`);
          NODE

      - name: Discover the canonical Homepage V4 browser contracts
        id: contracts
        shell: bash
        run: |
          set -euo pipefail
          files=(
            .github/workflows/homepage-cinematic-recovery-gate.yml
            .github/workflows/home-v4-loader-timing-gate.yml
          )
          mapfile -t scripts < <(
            grep -hoE 'node scripts/[A-Za-z0-9_.-]+\.cjs' "${files[@]}" \
              | awk '{print $2}' \
              | grep -Ei 'home|cinematic|loader|guided|journey|scene|viewport|responsive' \
              | awk '!seen[$0]++'
          )
          if [ "${#scripts[@]}" -eq 0 ]; then
            echo "No canonical Homepage gate scripts were discovered." >&2
            exit 1
          fi
          printf '%s\n' "${scripts[@]}" | tee /tmp/homepage-hosted-contracts.txt

      - name: Run every canonical Homepage V4 contract against the hosted alias
        id: matrix
        continue-on-error: true
        shell: bash
        env:
          AUDIT_BASE_URL: https://branding-tatva-git-homepage-cinematic-recovery-suman22.vercel.app
          BASE_URL: https://branding-tatva-git-homepage-cinematic-recovery-suman22.vercel.app
          SITE_URL: https://branding-tatva-git-homepage-cinematic-recovery-suman22.vercel.app
          PREVIEW_URL: https://branding-tatva-git-homepage-cinematic-recovery-suman22.vercel.app
          AUDIT_COMMIT: ${{ steps.source.outputs.sha }}
          HAWK_API_KEY: ""
        run: |
          set -uo pipefail
          failures=()
          : > /tmp/homepage-hosted-matrix.tsv
          while IFS= read -r script; do
            [ -n "$script" ] || continue
            if [ ! -f "$script" ]; then
              printf '%s\tmissing\n' "$script" | tee -a /tmp/homepage-hosted-matrix.tsv
              failures+=("$script:missing")
              continue
            fi
            echo "::group::${script}"
            if node "$script"; then
              outcome=success
            else
              outcome=failure
              failures+=("$script:failure")
            fi
            echo "::endgroup::"
            printf '%s\t%s\n' "$script" "$outcome" | tee -a /tmp/homepage-hosted-matrix.tsv
          done < /tmp/homepage-hosted-contracts.txt
          if [ "${#failures[@]}" -gt 0 ]; then
            printf 'Homepage hosted failures: %s\n' "${failures[*]}"
            exit 1
          fi

      - name: Report the complete hosted Homepage verification matrix
        if: always()
        shell: bash
        env:
          SOURCE_SHA: ${{ steps.source.outputs.sha }}
          MATRIX_OUTCOME: ${{ steps.matrix.outcome }}
        run: |
          set -euo pipefail
          {
            echo "## Hosted Homepage V4 verification matrix"
            echo
            echo "- Alias: https://branding-tatva-git-homepage-cinematic-recovery-suman22.vercel.app/"
            echo "- Exact deployable source: \`$SOURCE_SHA\`"
            echo "- Combined outcome: \`$MATRIX_OUTCOME\`"
            echo
            echo "| Contract script | Outcome |"
            echo "|---|---|"
            if [ -f /tmp/homepage-hosted-matrix.tsv ]; then
              while IFS=$'\t' read -r script outcome; do
                echo "| \`$script\` | $outcome |"
              done < /tmp/homepage-hosted-matrix.tsv
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          test "$MATRIX_OUTCOME" = success

      - name: Create an immutable hosted Homepage certification tag
        shell: bash
        env:
          CERTIFIED_SHA: ${{ steps.source.outputs.sha }}
        run: |
          set -euo pipefail
          short="${CERTIFIED_SHA:0:12}"
          tag="homepage-hosted-certified-${short}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git ls-remote --exit-code --tags origin "refs/tags/${tag}" >/dev/null 2>&1; then
            echo "Certification tag ${tag} already exists."
            exit 0
          fi
          git tag -a "$tag" "$CERTIFIED_SHA" -m "Homepage V4 certified on the permanent Vercel alias"
          git push origin "$tag"

      - name: Upload hosted Homepage evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: branding-tatva-homepage-hosted-${{ steps.source.outputs.sha }}
          path: |
            *home*audit/*
            *cinematic*audit/*
            *loader*audit/*
            homepage-*/**/*
            cinematic-recovery-audit/*
          if-no-files-found: warn
          retention-days: 14
