name: Build and verify the compressed deliverables archive
run-name: Services deliverables drawers pass 2 · ${{ github.sha }}

on:
  push:
    branches:
      - services-deliverables-drawers
    paths:
      - .github/workflows/services-deliverables-drawers.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: branding-tatva-services-deliverables-drawers
  cancel-in-progress: true

jobs:
  build-audit-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Check out the focused branch without the large media archive
        uses: actions/checkout@v4
        with:
          ref: services-deliverables-drawers
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /*
            !/public/videos/*
            !/public/audio/*

      - name: Apply the deliverables drawer browser contract
        run: |
          python -m py_compile scripts/apply_services_deliverables_drawers.py
          python scripts/apply_services_deliverables_drawers.py
          git diff --check

      - name: Use Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.28.0 --activate

      - name: Install dependencies and Chromium
        run: |
          pnpm install --frozen-lockfile
          pnpm add --save-dev --lockfile=false playwright@1.54.2
          pnpm exec playwright install --with-deps chromium

      - name: Build the production application
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: pnpm build

      - name: Start the production server
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          pnpm start --hostname 127.0.0.1 --port 3000 > /tmp/services-deliverables.log 2>&1 &
          echo $! > /tmp/services-deliverables.pid
          for attempt in {1..90}; do
            if curl --fail --silent http://127.0.0.1:3000/services > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/services-deliverables.log
          exit 1

      - name: Walk the complete Services journey at three viewports
        env:
          AUDIT_BASE_URL: http://127.0.0.1:3000
        run: node scripts/services_page_gate.cjs

      - name: Stop the production server
        if: always()
        run: |
          if [ -f /tmp/services-deliverables.pid ]; then
            kill "$(cat /tmp/services-deliverables.pid)" || true
          fi
          cat /tmp/services-deliverables.log || true

      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: services-deliverables-drawers-evidence
          path: services-page-audit/*
          if-no-files-found: warn
          retention-days: 7

      - name: Remove one-time transformer and workflow
        run: |
          rm scripts/apply_services_deliverables_drawers.py
          rm .github/workflows/services-deliverables-drawers.yml

      - name: Commit the verified deliverables archive
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            scripts/services_page_gate.cjs \
            scripts/apply_services_deliverables_drawers.py \
            .github/workflows/services-deliverables-drawers.yml
          git diff --cached --check
          git commit -m "Compress the Services deliverables into five drawers"
          git push origin HEAD:services-deliverables-drawers
