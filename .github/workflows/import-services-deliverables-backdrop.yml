name: Import original Services Deliverables backdrop

on:
  push:
    branches:
      - services-original-deliverables-backdrop
    paths:
      - .github/workflows/import-services-deliverables-backdrop.yml

permissions:
  contents: write

jobs:
  import-backdrop:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: Check out focused branch
        uses: actions/checkout@v4
        with:
          ref: services-original-deliverables-backdrop
          fetch-depth: 0

      - name: Download and validate the generated paper archive
        env:
          DELIVERABLES_IMAGE_URL: "https://photoshop-api.adobe.io/v2/short-url/urn:aaid:ps:US:7448897f-8f12-4d48-ae39-95d5c1867a91"
        run: |
          set -euo pipefail
          mkdir -p public/images/generated
          curl --fail --location --retry 4 --retry-all-errors \
            "$DELIVERABLES_IMAGE_URL" \
            --output public/images/generated/bt-services-deliverables-archive.png

          test -s public/images/generated/bt-services-deliverables-archive.png
          file public/images/generated/bt-services-deliverables-archive.png | grep -q "PNG image data"

          python <<'PY'
          from pathlib import Path
          import struct

          path = Path("public/images/generated/bt-services-deliverables-archive.png")
          data = path.read_bytes()
          if data[:8] != b"\x89PNG\r\n\x1a\n":
              raise SystemExit("Downloaded asset is not a PNG")
          width, height = struct.unpack(">II", data[16:24])
          if width < 2000 or height < 1100:
              raise SystemExit(f"Backdrop resolution is too small: {width}x{height}")
          print(f"Validated original Deliverables backdrop: {width}x{height}, {path.stat().st_size} bytes")
          PY

      - name: Commit the original paper archive
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/images/generated/bt-services-deliverables-archive.png
          git commit -m "Import original Services Deliverables archive"
          git push origin HEAD:services-original-deliverables-backdrop
