name: Retired homepage preview safety QA

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Point to the replacement gate
        run: echo "Superseded by home-preview-safety-qa-v2.yml"
