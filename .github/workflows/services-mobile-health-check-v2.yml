name: Build and verify the fresh-base compact mobile Health Check
run-name: Services mobile Health Check v2 · ${{ github.sha }}

on:
  push:
    branches:
      - services-mobile-health-check-v2
    paths:
      - .github/workflows/services-mobile-health-check-v2.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: branding-tatva-services-mobile-health-check-v2
  cancel-in-progress: true

jobs:
  build-audit-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Check out the fresh Services branch without the large media archive
        uses: actions/checkout@v4
        with:
          ref: services-mobile-health-check-v2
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /*
            !/public/videos/*
            !/public/audio/*

      - name: Install the compact Health Check and browser contract
        run: |
          python -m py_compile scripts/apply_services_mobile_health_check_v2.py
          python scripts/apply_services_mobile_health_check_v2.py
          git diff --check

      - name: Use Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.28.0 --activate

      - name: Install dependencies and Chromium
        run: |
          pnpm install --frozen-lockfile
          pnpm add --save-dev --lockfile=false playwright@1.54.2
          pnpm exec playwright install --with-deps chromium

      - name: Build the production application
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: pnpm build

      - name: Start the production server
        env:
          NEXT_TELEMETRY_DISABLED: 1
        run: |
          pnpm start --hostname 127.0.0.1 --port 3000 > /tmp/services-health-check-v2.log 2>&1 &
          echo $! > /tmp/services-health-check-v2.pid
          for attempt in {1..90}; do
            if curl --fail --silent http://127.0.0.1:3000/services > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/services-health-check-v2.log
          exit 1

      - name: Walk the complete Services journey at three viewports
        env:
          AUDIT_BASE_URL: http://127.0.0.1:3000
        run: node scripts/services_page_gate.cjs

      - name: Stop the production server
        if: always()
        run: |
          if [ -f /tmp/services-health-check-v2.pid ]; then
            kill "$(cat /tmp/services-health-check-v2.pid)" || true
          fi
          cat /tmp/services-health-check-v2.log || true

      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: services-mobile-health-check-v2-evidence
          path: services-page-audit/*
          if-no-files-found: warn
          retention-days: 7

      - name: Remove one-time transformation tools
        run: |
          rm scripts/apply_services_mobile_health_check_v2.py
          rm .github/workflows/services-mobile-health-check-v2.yml

      - name: Commit the verified mobile Health Check
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/app/services/page.tsx \
            src/sections/Services/BrandHealthCheck.tsx \
            src/sections/Services/HealthCheckMobileInstrument.tsx \
            scripts/services_page_gate.cjs \
            scripts/apply_services_mobile_health_check_v2.py \
            .github/workflows/services-mobile-health-check-v2.yml
          git diff --cached --check
          git commit -m "Compress the mobile Health Check into one live instrument"
          git push origin HEAD:services-mobile-health-check-v2
