export type CinematicHeroProps = {
  image?: string;
  video?: string;
  poster?: string;
  imagePosition?: string;
  badge: string;
  headline: React.ReactNode;
  subhead: string;
  children?: React.ReactNode;
};
