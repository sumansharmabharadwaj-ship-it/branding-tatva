export type HeaderProps = {
  // On hero pages the header starts see-through over the hero photo/video
  // and only gains its solid background once the visitor scrolls past it.
  transparent?: boolean;
};
