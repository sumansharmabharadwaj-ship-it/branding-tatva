# Homepage strategist reading and call path

## Change

The strategist chapter now separates its measured desktop layout from playback. Pausing motion preserves an established scroll frame and the selected discipline. Initial reduced motion uses ordinary flow; shorter viewports and content that grows beyond the viewport release the hold. A resumed scene retains its selection until a fresh scroll gesture takes over.

Keyboard reading settles the text, selection pill, result rule, media transition, portrait camera, and hover effects. The current film becomes its poster while reading is settled, and departing media layers are removed without replacing the tabs, proof link, or footer actions. Ambient colour animations pause outside the viewport and during keyboard reading. Pointer activation can resume motion.

The footer now contains both the strategist profile and a direct link to the existing 30 minute call section at `/contact#call`. Links can wrap on compact layouts. The portrait still explains that Suman leads the work directly.

The discipline labels use Applied psychology and Applied literature because `entityFactsPendingVerification` still lists exact degree wording and display permission as pending. Titles describe turning a position into a voice and putting decisions into practice. The Dr. Haley proof now names the documented Instagram result: 104% more followers per post while the schedule fell from 23 posts to 12. The previous sentence combined a LinkedIn engagement rate with the Instagram posting reduction.

## Verification

- TypeScript, component ESLint, and whitespace checks passed.
- The production build passed and generated 106 routes.
- The existing Studio scroll gate passed: forward/reverse progression, boundary deadband, short-runway handling, keyboard ownership, and motion fallbacks. Its portrait assertion now includes keyboard reading through `readingStill`.
- Eleven component lifecycle checks passed, covering hydration, initial reduced motion, pause/resume, content overflow, compact viewports, and widening while paused.
- The callback harness confirmed manual selection survives incidental scroll until fresh intent, a keyboard-focused proof link keeps its destination through wheel and scroll events, and selected text protects its reading. Focus leaving the chapter permits the timeline to resume.
- All three discipline proof destinations matched. Profile and call links were present for every discipline. Four tab key checks covered Left/Right wrapping and Home/End focus targets.
- Keyboard selection did not restart text animation. Its poster, stationary portrait, and settled pill were selected; pointer interaction restored the film and animated reading.
- Nine actual React/Framer Motion server renders passed: all three disciplines in full motion, reduced motion, and keyboard reading. Checked the single selected tab, matching panel label, unique IDs, two inert measurement groups, motion state, and all three destinations.
- Parsed CSS inspection found no remaining reduced motion geometry overrides for the studio in the homepage's imported stylesheets. The direct call target and its thirty minute description exist in the Contact source.

The callback harness executes component effects with mocked geometry, frame scheduling, and animation controls. Server rendering validates markup. Neither certifies browser layout, native focus traversal, film quality, or physical touch behavior.

## Pending controlled preview acceptance

The local browser previously rejected the production preview with `net::ERR_BLOCKED_BY_CLIENT`. No deployment is requested during the Vercel cooldown. On the next controlled preview containing this batch:

1. At 320px, 390px, 768px, 1180px, and desktop widths, inspect all three disciplines. Confirm the longer titles, proof, portrait caption, and both footer actions fit without clipping or horizontal overflow.
2. Scroll forward and backward through a desktop held scene. Pause midway, resume, and resize while paused. The chapter and following content should retain their position unless physical dimensions require ordinary flow.
3. Start with reduced motion enabled and repeat with enlarged text or a short viewport. The complete reading and every action should remain reachable.
4. Use Tab, Shift Tab, Left/Right, Home/End, Enter, and Space. Confirm text and media settle, focus remains visible, and the focused proof URL stays stable while scrolling.
5. Pause during a film transition and verify the current poster is the only remaining media layer. Resume and leave/reenter the chapter; no old reading animation should replay.
6. Follow each proof link, the profile link, and the call link. Confirm the call reaches the existing scheduling section and the About page is unchanged.

This batch is saved preview source. Publication and visual acceptance remain pending.
