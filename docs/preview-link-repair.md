# Update the Branding Tatva preview link

Release 414 is READY and includes the verified homepage correction from
release 413 plus the concurrent Services update. The hidden cost chapter now explains branding
directly through a brand consultancy example. Recipe and meal planning copy
has been removed. “Mixed messages” and “Clear positioning” compare the three
channels around a clear reason to choose the business. The section's existing
animation is preserved. The earlier service path, mobile menu, Insights and
Services improvements are included.

[Branding Tatva preview](https://branding-tatva-git-august-8-isolated-suman22.vercel.app/).

The shared link was successfully updated to release 408 following the user's
earlier command. It still points to that release, trigger
`30683d854ee271602773e892cd90c03c32dd9110`. The connected tools expose deployment
inspection but no alias assignment. In the Terminal already signed into the
site's Vercel account, run:

```bash
npx --yes vercel@59.19.0 alias set dpl_5C3ec1h8SuLB7QBF4G2afRzTJd1s branding-tatva-git-august-8-isolated-suman22.vercel.app --scope suman22
```

This changes the shared preview link without promoting production. Release
414's exact trigger is `19d0e91b5bce5c61ad6257662270433f0d58eeff`.

TypeScript, changed-file ESLint, homepage source, type floor, production build and
rendered homepage checks passed. All three final release workflows passed.
The exact release 413 deployment passed desktop static layout and narrow layout
checks, mobile keyboard comparison switching and stable comparison sizing.
Pointer and desktop keyboard dispatch timeouts limited further interaction
checks. See `homepage-brand-message-copy-qa.md` for exact measurements and
verification limits. Release 414 inherits this homepage component unchanged;
its additional edits are confined to Services and the release number.

The browser's /api/release restriction remains, so endpoint certification is
unavailable. Metadata, source to trigger comparisons, and rendered changes
establish the exact previews used for verification.

A later preview needs its own confirmed deployment target. Automatic alias
assignment requires the repository's existing Vercel credential to be configured.
